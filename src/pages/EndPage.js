import backgroundEndPage from '../images/Final1080.gif';
import '../styles/components/EndPage.scss';

function EndPage () {
    return(
        <>
        <header>

        </header>
        <main>
        <img className="backgroundEndPage" src={backgroundEndPage} alt="escenario del juego" />
        </main>
        </>
    )
}

export default EndPage;